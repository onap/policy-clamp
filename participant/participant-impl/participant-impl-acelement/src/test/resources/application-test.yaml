spring:
  security:
    user:
      name: acmUser
      password: zb!XztG34

server:
  port: 8084
  error:
    path: /error

element:
  elementId:
    name: onap.policy.clamp.ac.element1
    version: 1.0.0
management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: health, metrics, prometheus
